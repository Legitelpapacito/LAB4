package model;

// Editor. Crea y edita, pero no publica ni elimina.
public class Editor extends Usuario {
    public Editor(String nombre){
        super(nombre);
    }
    // Hereda los permisos en false
}
